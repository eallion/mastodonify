{
  "name": "mastodonify",
  "version": "1.0.6",
  "description": "A Chrome/Firefox extension that shows Mastodon notification count as a badge",
  "scripts": {
    "build": "pnpm run build:chrome && pnpm run build:firefox",
    "build:chrome": "node build/build.js chrome",
    "build:firefox": "node build/build.js firefox",
    "package": "pnpm run build && node build/package.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tslib": "^2.8.1",
    "web-ext": "^7.12.0",
    "zip-a-folder": "^3.1.9"
  },
  "packageManager": "pnpm@8.15.0"
}